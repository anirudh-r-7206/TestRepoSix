void Dev.Delete_All_Records()
{

                 	// 		//	Masters
                 	// 		delete from Add_Country[ID != 0];
                 	// 		delete from Freight_Partners_Form[ID != 0];
                 	// 		delete from Currency_Conversion_Rates[ID != 0];
                 	// 		delete from Rate_Conversion_History [ ID != 0 ];
                 	// 		delete from Fabric_Form[ID != 0];
                 	// 		delete from Style[ID != 0];
                 	// 		delete from Product_Code_Master[ID != 0];
                 	// 		delete from Create_Size[ID != 0];
                 	// 		delete from Size_Chart_Mapping[ID != 0];
                 	// 		delete from Product_Category[ID != 0];
                 	// 		delete from Wardrobe[ID != 0];
                 	// 		delete from Add_Colors[ID != 0];
                 	// 		//	User Master
                 	// 		delete from Create_User[ID != 0];
                 	// 		delete from Add_Suppliers[ID != 0];
                 	// 		delete from Buyers_Master[ID != 0];
                 	// 		delete from Buyer_Product_Mapping[ID != 0];
                 	// 		delete from Buyer_Products[ID != 0];
                 	// 		// Order Master
                 	// 		delete from Add_Product[ID != 0];
                 	// 		delete from Packaging_Details[ID != 0];
                 	// 		delete from Order_Request[ID != 0];
                 	// 		delete from Order_Details[ID != 0];
                 	// 		delete from Purchase_Order[ID != 0];
                 	// 		delete from Purchase_LineItems[ID != 0];
                 
}