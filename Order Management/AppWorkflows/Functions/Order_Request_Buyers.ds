list Buyer.Order_Request_Buyers()
{

                 	// 	fetBuyers = Order_Request[ID != null];
                 	fetBuyers = Create_User[ID != 0];
                 	if(fetBuyers.count() > 0)
                 	{
                 		return fetBuyers.Associated_Buyer.getall();
                 	}
                 	else
                 	{
                 		return List();
                 	}
                 
}