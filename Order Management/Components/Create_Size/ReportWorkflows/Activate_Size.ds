Activate_Size as "Activate Size"
{

				type =  functions
				form = Create_Size
				execution type = for each record	
				on start
				{
					actions 
					{
					on click
					(
						input.Status = "Active";
					)
					}
				}
			
}