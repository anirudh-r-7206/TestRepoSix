Hide_Fields4 as "Hide Fields"
{

				type =  form
				form = Email_Template

				record event = on add or edit

				on load
				{
					actions 
					{
						custom deluge script
						(
											hide Notification_Type;
						)
					}
				}

			
}