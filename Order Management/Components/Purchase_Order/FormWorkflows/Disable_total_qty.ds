Disable_total_qty as "Disable total qty"
{

				type =  form
				form = Purchase_Order

				record event = on add or edit

				field rules
				{
					actions 
					{
						disable Total_Quantity;
					}
				}

			
}