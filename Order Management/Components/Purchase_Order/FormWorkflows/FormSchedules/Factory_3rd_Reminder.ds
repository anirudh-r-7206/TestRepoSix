Factory_3rd_Reminder as "Factory_3rd_Reminder"
{

				type =  schedule
				form = Purchase_Order[Order_Status == "Awaiting Factory Review"]
				start = after 144 hours from Modified_Time
				time zone = "Asia/Kolkata"
				on start
				{
					actions 
					{
					on load
					(
						thisapp.Send_Mail.Email_Template(input.ID,"Factory_3rd_Reminder");
					)
					}
				}
			
}