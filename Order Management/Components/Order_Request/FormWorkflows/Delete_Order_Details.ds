Delete_Order_Details as "Delete Order Details"
{

				type =  form
				form = Order_Request

				record event = on delete

				on success
				{
					actions 
					{
						custom deluge script
						(
											delete from Order_Details[Order_Request == null || Order_Request == input.ID];
						)
					}
				}

			
}