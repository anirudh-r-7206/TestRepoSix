Delete_Selected_Wardrobes as "Delete Selected Wardrobes"
{

				type =  functions
				form = Wardrobe
				execution type = for each record	
				on start
				{
					actions 
					{
					on click
					(
						WardrobesList = thisapp.Delete_Records.Delete_Wardrobes();
										if(!WardrobesList.contains(input.ID))
										{
											// your logic here
											delete from Wardrobe[ID == input.ID];
										}
					)
					}
				}
			
}