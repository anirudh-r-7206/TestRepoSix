Raise_Order as "Raise Order"
{

				type =  functions
				form = Buyers_Master
				execution type = for each record	
				on start
				{
					actions 
					{
					on click
					(
						openUrl("#Form:Order_Request?Buyer=" + input.ID,"popup window","height=90%,width=90%");
					)
					}
				}
			
}