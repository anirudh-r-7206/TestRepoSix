Approve_Supplier as "Approve Supplier"
{

				type =  functions
				form = Add_Suppliers
				execution type = for each record	
				on start
				{
					actions 
					{
					on click
					(
						input.Approval_Status = "Approved";
										input.Status = "Active";
					)
					}
				}
			
}