Delete_Selected_Styles as "Delete Selected Styles"
{

				type =  functions
				form = Style
				execution type = for each record	
				on start
				{
					actions 
					{
					on click
					(
						StyleList = thisapp.Delete_Records.Delete_Styles();
										if(!StyleList.contains(input.ID))
										{
											// your logic here
											delete from Style[ID == input.ID];
										}
					)
					}
				}
			
}