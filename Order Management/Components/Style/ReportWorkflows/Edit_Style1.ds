Edit_Style1 as "Edit Style"
{

				type =  functions
				form = Style
				execution type = for each record	
				on start
				{
					actions 
					{
					on click
					(
						// reference = thisapp.References.Style(input.ID);
										// if(reference.trim() != "" && reference != null)
										// {
										// 	url = "#Form:Alert_References?zc_Header=false&Style=" + input.ID;
										// 	window = "popup window";
										// }
										// else
										// {
										url = "#Form:Style?zc_LoadIn=dialog&viewLinkName=All_Styles&recLinkID=" + input.ID;
										window = "same window";
										// }
										openUrl(url,window);
					)
					}
				}
			
}