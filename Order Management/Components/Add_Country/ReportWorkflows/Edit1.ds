Edit1 as "Edit"
{

				type =  functions
				form = Add_Country
				execution type = for each record	
				on start
				{
					actions 
					{
					on click
					(
						reference = thisapp.References.Country(input.ID);
										// if(reference.trim() != "" && reference != null)
										// {
										// 	url = "#Form:Alert_References?zc_Header=false&Add_Country=" + input.ID;
										// 	window = "popup window";
										// }
										// else
										// {
										url = "#Form:Add_Country?zc_NextUrl=#Script:page.refresh&zc_LoadIn=dialog&viewLinkName=All_Countries&recLinkID=" + input.ID;
										window = "same window";
										// }
										openUrl(url,window);
					)
					}
				}
			
}