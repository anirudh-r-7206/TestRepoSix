Size_filter_by_Wardrobe as "Size filter by Wardrobe"
{

			
							type =  form
							form = Add_Product
			
							record event = on add or edit
								status = inactive
			
							on user input of Wardrobe
							{
								actions 
								{
									custom deluge script
									(
														// if(input.Product_Category != null && !input.Gender.isEmpty())
													// {
													// 	get_One_Size = Create_Size[Product_Category == input.Product_Category && Gender == input.Gender && Status == "Active" && Size.contains("One Size")];
													// 	getAll_Size = Create_Size[Product_Category == input.Product_Category && Gender == input.Gender && Status == "Active"].ID.getAll();
													// 	if(getAll_Size.size() > 0)
													// 	{
													// 		get_Product_Size = Add_Product[Wardrobe == input.Wardrobe && Product_Category == input.Product_Category && Style_Code == input.Style_Code && Product_Colour == input.Product_Colour].Size_Set.getAll();
													// 		get_Product_One_Size = Add_Product[Wardrobe == input.Wardrobe && Product_Category == input.Product_Category && Style_Code == input.Style_Code && Product_Colour == input.Product_Colour && Size_Set.Size.contains("One Size")];
													// 		getAll_Size.removeAll(get_Product_Size);
													// 		if(get_Product_One_Size.count(ID) > 0)
													// 		{
													// 			clear Size_Set;
													// 		}
													// 		else
													// 		{
													// 			if(get_Product_Size.size() > 0)
													// 			{
													// 				getAll_Size.removeElement(get_One_Size.ID);
													// 			}
													// 			if(input.Size_Set != null)
													// 			{
													// 				this_Rec_Size = Add_Product[ID == input.ID];
													// 				if(this_Rec_Size.count() > 0)
													// 				{
													// 					old_Size = input.Size_Set;
													// 					getAll_Size.add(old_Size);
													// 				}
													// 				input.Size_Set:ui.add(getAll_Size.distinct());
													// 				input.Size_Set = old_Size;
													// 			}
													// 			else
													// 			{
													// 				input.Size_Set:ui.add(getAll_Size);
													// 			}
													// 			if(input.Gender == "Unisex" && get_Product_Size.size() == 0)
													// 			{
													// 				input.Size_Set = get_One_Size.ID;
													// 			}
													// 		}
													// 	}
													// 	else
													// 	{
													// 		clear Size_Set;
													// 	}
													// }
													// else
													// {
													// 	clear Size_Set;
													// }
									)
								}
							}
			
						
			
}