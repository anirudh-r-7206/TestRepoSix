Edit_Product as "Edit Product"
{

			
							type =  functions
							form = Add_Product
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									// reference = thisapp.References.Product(input.ID);
													// if(reference.trim() != "" && reference != null)
													// {
													// 	url = "#Form:Alert_References?zc_Header=false&Add_Product=" + input.ID;
													// 	window = "popup window";
													// }
													// else
													// {
													url = "#Form:Add_Product?zc_LoadIn=dialog&viewLinkName=Products_Report&recLinkID=" + input.ID;
													window = "same window";
													// }
													openUrl(url,window);
								)
								}
							}
						
			
}