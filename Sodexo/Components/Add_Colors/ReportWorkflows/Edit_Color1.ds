Edit_Color1 as "Edit Color"
{

			
							type =  functions
							form = Add_Colors
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									// reference = thisapp.References.Color(input.ID);
													// if(reference.trim() != "" && reference != null)
													// {
													// 	url = "#Form:Alert_References?zc_Header=false&Add_Colors=" + input.ID;
													// 	window = "popup window";
													// }
													// else
													// {
													url = "#Form:Add_Colors?zc_LoadIn=dialog&viewLinkName=All_Colors&recLinkID=" + input.ID;
													window = "same window";
													// }
													openUrl(url,window);
								)
								}
							}
						
			
}