Delete_Selected_Countries as "Delete Selected Countries"
{

			
							type =  functions
							form = Add_Country
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									deleteCountryList = thisapp.Delete_Records.Delete_Countries();
													if(!deleteCountryList.contains(input.ID))
													{
														// your logic here
														delete from Add_Country[ID == input.ID];
													}
								)
								}
							}
						
			
}