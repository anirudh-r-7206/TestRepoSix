Delete_workflow as "Delete"
{

			
							type =  functions
							form = Add_Country
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									reference = thisapp.References.Country(input.ID);
													if(reference.trim() != "" && reference != null)
													{
														url = "#Form:Alert_References?zc_Header=false&Add_Country=" + input.ID;
														window = "popup window";
														openUrl(url,window);
													}
													else
													{
														delete from Add_Country[ID == input.ID];
													}
								)
								}
							}
						
			
}