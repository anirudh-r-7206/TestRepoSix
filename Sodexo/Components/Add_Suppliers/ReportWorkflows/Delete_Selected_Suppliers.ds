Delete_Selected_Suppliers as "Delete Selected Suppliers"
{

			
							type =  functions
							form = Add_Suppliers
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									deleteSupplierList = thisapp.Delete_Records.Delete_Suppliers();
													if(!deleteSupplierList.contains(input.ID))
													{
														// your logic here
														delete from Add_Suppliers[ID == input.ID];
													}
								)
								}
							}
						
			
}