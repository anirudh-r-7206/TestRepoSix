Update_QO_generation as "Update QO generation"
{

			
							type =  form
							form = Order_Request
			
							record event = on edit
			
							on success
							{
								actions 
								{
									custom deluge script
									(
														get_User = Create_User[Email == zoho.loginuserid && Status == "Active"];
													if(get_User.User_Type != "Buyer")
													{
														// 	Supplier_Count = 0;
														// 	for each  details in input.Order_details
														// 	{
														// 		if(details.Supplier == null)
														// 		{
														// 			Supplier_Count = Supplier_Count + 1;
														// 		}
														// 	}
														// 	if(Supplier_Count == 0)
														// 	{
														input.Status = "Awaiting QO Generation";
														// History Push
														get_User = Create_User[Email == zoho.loginuserid && Status == "Active"];
														HistoryPush = insert into History
														[
															Added_User=zoho.loginuser
															Order_ID=input.ID
															Raised_By=get_User.ID
															Status=input.Status
															Comments="Order Request " + input.Order_ID + " has been moved to '" + input.Status + "' by " + get_User.User_Name + "."
														];
														openUrl("#Script:page.refresh","same window");
														// 	}
													}
									)
								}
							}
			
						
			
}