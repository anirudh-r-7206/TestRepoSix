Generate_QO as "Generate QO"
{

			
							type =  functions
							form = Order_Request
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									// get_Suppliers = Order_Details[Order_Request == input.ID].Supplier.getAll().distinct();
													// if(get_Suppliers.size() > 1)
													// {
													// 	try 
													// 	{
													// 		get_User = Create_User[Email == zoho.loginuserid && Status == "Active"];
													// 		for each  supply in get_Suppliers
													// 		{
													// 			product_List = Collection();
													// 			get_Product = Order_Details[Order_Request == input.ID && Supplier == supply];
													// 			s_No = 0;
													// 			total_Quantity = 0;
													// 			total_Purchase_Price = 0;
													// 			total_selling_Price = 0;
													// 			for each  item in get_Product
													// 			{
													// 				s_No = s_No + 1;
													// 				get_product_Price = Buyer_Products[Buyer_Product_Mapping.Buyer == input.Buyer_Organisation && Style_ID == item.Product && Buyer_Product_Mapping.Factory_Allocated == supply];
													// 				// 	Cunstruct Subform ;
													// 				sub_Form = Purchase_Order.Purchase_LineItems();
													// 				sub_Form.S_No=s_No;
													// 				sub_Form.Product=item.Product;
													// 				sub_Form.Style_Code=item.Product.Style_Code.Style_Code;
													// 				sub_Form.Product_Color=item.Product_Color;
													// 				sub_Form.Product_Size=item.Product_Size;
													// 				sub_Form.Description=item.Description;
													// 				sub_Form.Quantity=item.Quantity;
													// 				sub_Form.Purchase_Price=if(get_product_Price.count(ID) > 0,get_product_Price.Buy_Price,0);
													// 				sub_Form.Selling_Price=if(get_product_Price.count(ID) > 0,get_product_Price.Sell_Price,0);
													// 				sub_Form.Purchase_Price_Sub_Total=item.Quantity * if(get_product_Price.count(ID) > 0,get_product_Price.Buy_Price,0);
													// 				sub_Form.Selling_Price_Sub_Total=item.Quantity * if(get_product_Price.count(ID) > 0,get_product_Price.Sell_Price,0);
													// 				product_List.insert(sub_Form);
													// 				// 	Calculate Total Values
													// 				total_Quantity = total_Quantity + item.Quantity;
													// 				total_Purchase_Price = total_Purchase_Price + item.Quantity * if(get_product_Price.count(ID) > 0,get_product_Price.Buy_Price,0);
													// 				total_selling_Price = total_selling_Price + item.Quantity * if(get_product_Price.count(ID) > 0,get_product_Price.Sell_Price,0);
													// 			}
													// 			// 			info product_List;
													// 			add_PO = insert into Purchase_Order
													// 			[
													// 				Added_User=zoho.loginuser
													// 				Order_Type="Bulk"
													// 				Delivery_Date=zoho.currentdate
													// 				Selling_Terms="COD"
													// 				Transport_Mode="SEA"
													// 				Purchase_Currency=input.Buyer_Organisation.Currency
													// 				Supplier=supply
													// 				Order_Request=input.ID
													// 				Buyer_Organisation=input.Buyer_Organisation
													// 				Customer_Name=input.Buyer_Name
													// 				Country=input.Buyer_Organisation.Country
													// 				Order_Status="Awaiting QO Generation"
													// 				Total_Purchase_Price=total_Purchase_Price
													// 				Total_Selling_Price=total_selling_Price
													// 				Total_Quantity=total_Quantity
													// 				Purchase_LineItems=product_List
													// 			];
													// 			get_PO = Purchase_Order[ID == add_PO];
													// 			// Purchase History Push
													// 			HistoryPush = insert into History
													// 			[
													// 				Added_User=zoho.loginuser
													// 				Purchase_Order_ID=add_PO
													// 				Raised_By=get_User.ID
													// 				Status="Awaiting QO Generation"
													// 				Comments="Purchase Order " + get_PO.PO_Number + " has been raised by " + get_User.User_Name + "."
													// 			];
													// 		}
													// 		input.Status = "Validation Pending";
													// 		// Order History Push
													// 		HistoryPush = insert into History
													// 		[
													// 			Added_User=zoho.loginuser
													// 			Order_ID=input.ID
													// 			Raised_By=get_User.ID
													// 			Status=input.Status
													// 			Comments="Order Request " + input.ID + " has been moved to '" + input.Status + "' by " + get_User.User_Name + "."
													// 		];
													// 		// 		thisapp.PageRefresh();
													// 		openUrl("#Report:All_Purchase_Orders?Order_Request=" + input.Order_ID,"popup window","height=<height>,width=<width>");
													// 		// 		openUrl( "#Report:All_Purchase_Orders?Order_Request="+input.Order_ID, "new window");
													// 	}
													// 	catch (e)
													// 	{
													// 		info e;
													// 	}
													// }
													// else
													// {
													openUrl("#Form:Purchase_Order?Order_Request=" + input.ID,"popup window");
													// }
								)
								}
							}
						
			
}