Edit_PO as "Edit PO"
{

			
							type =  functions
							form = Purchase_Order
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									// Edit PO
													// openUrl("#Form:Purchase_Order?recLinkID=" + input.ID + "&viewLinkName=All_Purchase_Orders","popup window");
													openUrl("#Page:Purchase_Order_Details?zc_LoadIn=embed-editPO&PO_ID=" + input.ID,"same window");
								)
								}
							}
						
			
}