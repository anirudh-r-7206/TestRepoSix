Edit_Product_category as "Edit Product category"
{

			
							type =  functions
							form = Product_Category
							execution type = for each record	
							on start
							{
								actions 
								{
								on click
								(
									// reference = thisapp.References.ProductCategory(input.ID);
													// if(reference.trim() != "" && reference != null)
													// {
													// 	url = "#Form:Alert_References?zc_Header=false&Product_Category=" + input.ID;
													// 	window = "popup window";
													// }
													// else
													// {
													url = "#Form:Product_Category?zc_LoadIn=dialog&viewLinkName=All_Product_Categories&recLinkID=" + input.ID + "&zc_NextUrl=#Script:page.refresh";
													window = "same window";
													// }
													openUrl(url,window);
								)
								}
							}
						
			
}